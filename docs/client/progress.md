# Client planning progress

## 2026-09-23 — Map Lab zone colours and continuous painting

- Added an independent native overlay-colour picker for each development zone. Colours update fills, dashed outlines and labels immediately and stay with the zone across selection changes; like all administration edits, they reset on regeneration/reload. No persistence or production game mechanics added.
- Add/Remove now paints on pointer-down and continuously through movement, sampling between sparse events and deduplicating repeated brush centres. Existing 1/7/19-cell brushes, exclusive provinces, overlapping zones and country/water restrictions remain unchanged. Right/middle-drag pans while editing; Inspect retains left-drag panning. Coordinates remain the keyboard/exact-cell alternative.
- Boundary history records one entry per non-empty stroke (50 maximum), preserving original province assignments. Escape, pointer cancellation, outside release, lost capture and window blur stop painting and keep already-painted cells undoable. Scope/control changes end the active stroke before changing targets. Colour edits do not alter membership or invalidate cached geometry.
- The UI skill guided native labelled controls, existing semantic styling and a lab-local implementation; no new dependency, shared-renderer change, backend or live data write. Updated the Map Lab handoff and UI foundation catalog.
- Verification: production build, formatting and all 34 client Node test files passed, including colour validation/isolation, overlapping-brush undo, target guards and bounded history. All 15 administration/map/military Chromium scenarios passed (3.2 minutes), including fast movement, painting before release, add/remove undo, Escape/cancel/outside release, colour retention, Terrain V2, narrow 22,200-cell layout and existing formation/order interactions. Reviewed `test-results/client/map-administration-paint-colour.png`. Physical-device/non-Chromium checks remain open.


## 2026-09-23 — Flag Editor Lab and independent randomizers

- Delivered the approved [Identity Lab](../game-design/identity-lab.md) at `/dev-panel/identity-lab`, linked from Tools. Flag Editor is active; Emblem Editor and Coat of Arms Editor remain visibly disabled. The user approved the initial six-composition proof, requested lower editing friction and expects nation creation to be a likely later consumer. This delivery has no game data reads/writes, uploads, backend migration or live flag assignment.
- Added 15 starting recipes across 12 pattern families, editable/clipped geometry, 14 original bundled symbols, constrained local SVG import, 900 × 600 opaque PNG and editable versioned JSON. Retained schema/renderer v1; v2 upgrades detach saved originals. Stored PNGs remain viewable when their recipes are unsupported. [Heraldry research](../game-design/identity-lab-heraldry-research.md) is preserved separately; no Armoria runtime or copied third-party artwork/code was introduced.
- Simplified the primary workflow to visual patterns, swatches, optional emblem and direct dragging. Independent shapes/colours/emblem randomizers preserve other choices; undo/redo and exact Advanced controls remain available. Generated monochromatic, analogous, complementary and split-complementary palettes replace the four-preset randomizer. This is a small colour-generation function, not a new colour-wheel component. Six seeded alternatives support locks; symbols support row/ring arrangements.
- Added an eight-study IndexedDB shelf with atomic recipe/PNG snapshots. Failed saves preserve old records; edits during saving remain unsaved; late imports cannot replace a newer design. Native/shared controls and Scope provide EN/FR, keyboard alternatives, object-URL/listener cleanup and narrow layout.
- Verification: all 8 focused Node cases and 11 Chromium scenarios passed, including 1,000 distinct palettes with tone separation, independent/seeded randomization, SVG holes/rejections, local save/reload/failure/races, exact PNG/JSON pixel restoration, drag/keyboard undo, eight studies, disabled tabs and mobile French. The prior isolated proof also passed all 9 existing Portrait Lab scenarios. Production build, generated-source/PHP route contracts and formatting passed. Reviewed desktop, randomized and narrow screenshots.
- Verified the real development page returns HTTP 200 and, in a fresh browser, all three randomizers, PNG export and local save/reload work on its ordinary LAN HTTP origin with no page errors. IDs use `getRandomValues`, since `randomUUID` is unavailable on that origin. Three observed click-to-PNG-download times were 61–70 ms in headless Chromium; these are local samples, not device/browser guarantees.
- Measured the final initial dependency graph at 6 JS/CSS files, 80,945 bytes raw and 28,907 bytes with local gzip estimation; original catalog source is 1,308 bytes gzipped. No per-symbol requests, icon font, remote artwork or deferred-editor assets load. Screenshots live under `test-results/client/identity-lab-*.png`.
- Updated the [plan](../game-design/identity-lab-plan.md), handoff, foundation catalog, index and [ADR 0019](decisions/0019-experimental-flag-editor.md). Remaining evaluation is the user's judgement of final UI/flag quality. Palette/layout contrast is heuristic, the SVG subset is deliberately limited, and cross-browser/physical-device behavior is not yet verified. Game integration requires a separate implementation plan.

## 2026-09-23 — Identity Lab narrowed to a flag proof

- Revised the [implementation plan](../game-design/identity-lab-plan.md) following the user's observation that the game currently supports only flags. The new experiment has three visible tabs: Flag Editor enabled, Emblem Editor and Coat of Arms Editor disabled and deferred.
- Replaced the two-design/two-PNG project with a flag-only recipe and one finished PNG. The first milestone proves geometry, colour, symbol placement and editable JSON/PNG restoration against solid-colour references, with user visual evaluation before full editor expansion. No live flag replacement or game integration is included.
- Preserved prior Armoria/Heraldicon research, artwork/font candidates, traditional/modern template ideas and multi-design considerations in [deferred research notes](../game-design/identity-lab-heraldry-research.md). Armoria extraction and a large heraldic asset catalog no longer gate the flag proof. The earlier broader planning entry below is historical and superseded for delivery scope.
- Updated the client index and foundation catalog. Checked document links and active/deferred scope consistency; this was documentation-only, with no runtime tests, dependencies or implementation changes.

## 2026-09-23 — Map Lab provinces and overlapping development zones

- Implemented the approved map-only experiment: fixed microcell countries, editable exclusive provinces and overlapping named development zones. Land adjacency partitions seed up to three countries/provinces per country; islands attach by proximity. Initial coastal/river-network zones cross province borders and overlap. All membership is lab-local and independent of geography names, original ownership/occupation fixtures and production accounts.
- Added optional administration view, independent border/zone filters, country and area lookup, text-safe creation/renaming, 1/7/19-cell click stamps, coordinate-form alternative, 50-stamp boundary undo and confirmed political-only reset. Inspection reports country, province, all zones and underlying large hex. Drag still pans; Escape stops editing. Diagnostic views disable editing and suppress the overlay; returning to existing demos preserves their preferences/data.
- The UI skill guided native controls, semantic field styling, readable inspector/list and focus preservation. No new shared widget, backend, live-game write, dependency, custom-world-size implementation or policy/economic engine. Islands/disconnected pieces are allowed; water is excluded; regeneration/reload resets the fixture. These are provisional experiment choices, not production rules.
- Verification: all 33 client Node files passed, followed by the focused administration rerun after per-area geometry caching. Production build, formatting, the combined 23-scenario Chromium map regression, and a final three-scenario rebuilt administration rerun passed. Coverage includes isolation, overlaps, undo/reset, focus preservation, hidden-layer and navigation editing guards, Terrain V2 stamping/panning/zoom, narrow layout and 22,200 cells. Reviewed world/close-up/narrow screenshots; country labels take overview priority and unchanged area geometry is reused across edits. Initial geometry preparation still has an upfront cost; no guaranteed frame rate or physical-device/non-Chromium claim. Details and limits: [Map Lab](../game-design/map-lab.md), [geographic identity boundary](decisions/0016-geographic-feature-atlas.md).

## 2026-09-23 — Flag and coat of arms Lab implementation plan

- Scope clarification from the user: this is a new standalone experimental Lab module, not a game feature. Every planned phase ends at Lab evaluation; any future game integration needs a separate request and plan. Updated the plan's boundary and removed automatic-follow-on wording.
- Recorded the user's discussed direction and a proposed phased implementation in [Identity Lab plan](../game-design/identity-lab-plan.md): traditional/modern emblems, geometric 3:2 flags, nation-colour inputs, shared bundled symbols, editable JSON and finished PNG assets.
- Separated a bounded Armoria/code/artwork reuse evaluation from the standalone browser-local Lab and later gameplay integration. Documented the C# flag reference as an adaptation candidate, with its source reuse terms still to inspect before copying code.
- Specified proposed recipe/version ownership, embedded-emblem relationships, transparent emblem versus opaque flag output, snapshot-consistent local saving, import bounds, focused checks and review gates. Linked the plan from the client index and foundation catalog; no runtime files or dependencies changed.
- Verification: read current portrait sources, route/navigation/test entry points and planning conventions; reviewed the document links and planned-vs-implemented wording. No runtime tests apply to this documentation-only pass. Next step is user review of the plan, followed by Phase 0 when implementation is authorized.

## 2026-09-23 — Compact orders, mobile picking and header navigation

- Made nation creation the only colour-selection flow. Removed the game-menu editor, `NationIdentity`, its gameplay command and `POST /nation/colors`; persisted assignments and public colour rendering remain unchanged.
- Reordered the game header to Novus mark, hamburger, icon-only World/Nation/Economy/Military/Reports links and turn context. Games now lives in the hamburger menu. Localized labels remain as accessible names and titles.
- Grouped same-type, same-territory units into counted artwork stacks, replaced the common actions with an accessible icon toolbar and condensed per-unit route repetition into reachability and route groups with on-demand details. The list destination remains as a collapsed keyboard fallback.
- On narrow screens, opening Move/attack collapses the bottom sheet to a short map-picking prompt. Selecting a territory reopens the review and confirmation content; choosing another destination contracts it again. Existing command context, costs, validation, reconciliation and no-retry behavior are unchanged.
- Verification: generated client and PHP contracts, production build, all 32 Node suites, focused Chromium screenshots, and the complete 114-test Chromium suite passed. The final suite covers persistent panels, unit control, localization, game switching, entry, administration-facing routes and labs together.

## 2026-09-22 — Selectable AI scripts and standalone author kit

- Added trusted local PHP script discovery, a per-nation admin selector and script-specific notebooks. The current strategy lives in one selectable `experimental-v1.php`; switching preserves orders/readiness and fences stale decisions. Existing assignments retain V1 by default.
- Shared owner workspace exports and authoritative commands with the player API. Scripts receive plain snapshots and real forecast helpers, return an action plan and never need the application source. Bounded child processes contain script timeouts/fatal errors; they are not a security sandbox. Invalid custom decisions roll back completely before one V1 fallback, preserving selection and notebooks. No model/provider-specific infrastructure.
- Admin downloads include an explicitly selected private player snapshot and a standalone author ZIP with skill, documentation, starter, actual V1, synthetic snapshots and offline structural/forecast checks. Preview is not a full legality simulation. Bot-owner clients can keep driving the loop, without human Auto-ready acting on automated nations.
- Verification passed: new isolated script/fallback/notebook/rollback checks, existing real allocator/protection/control/atomicity and two-process duplicate checks, focused Node suites and AI Chromium fixtures, a real isolated admin browser journey including downloads and narrow layout, PHP syntax/contracts, generated-source checks and production build. Extracted starter and V1 run without the application autoloader; skill validation passed. All gameplay test mutations used the disposable database, not live games.
- Installed only the additive script-selection migration in the workspace application; no live turn was advanced or nation reassigned. [ADR 0018](decisions/0018-local-ai-scripts.md) and the module handoff record scope and complete removal. Long-match balance remains in-game playtesting.

## 2026-09-22 — Reversible experimental AI control

- Administration can now assign any manual nation in an existing AI-enabled game—including the administrator's own nation—to a fresh cautious, balanced or aggressive V1 controller, and release it again later.
- Assignment preserves the nation, account, existing orders and current readiness; a Planning nation can act in the current turn while a Ready nation begins under AI on the next turn. Release keeps its prior behavior of returning the nation to Planning for immediate manual play. The server retains game membership checks, generation fencing, the game lock and the 10-controller limit.
- The existing AI administration composition now refreshes its release/assignment choices after each transfer and uses shared field/button/confirmation foundations. No schema, provider framework or permanent game rule was added.
- Verification passed: PHP/JavaScript syntax, Prettier, all 12 Node client suites, no-database route contracts, production build and the focused database-backed AI checks, including release → reassign → release. Database mutations used only the disposable socket-only fixture; the temporary server was stopped afterward.

## 2026-09-22 — Multi-game stage 5: legacy interface retired

- Made the new client the default from `/`. Retired `/login`, `/dashboard`, `GET /create-nation` and `/dev-panel` bookmarks redirect to the new entry, Games or Administration destinations while preserving authentication/development boundaries and selected game context where applicable.
- Deleted the Blade login, dashboard, nation form, development panel and API-console SPA; removed their components and jQuery/dashboard/map browser assets. Removed the old development routes for game/turn/user/password/object operations because current administration already provides them. Current labs remain under Tools & experiments.
- Removed downloadable/generated jQuery services and the static legacy generator. The deterministic ES-module endpoint catalogue and its build command remain. The modern multipart nation operation at `POST /create-nation` remains because the new wizard owns it; shared gameplay/backend operations were not removed.
- Verification: no-database route/source/generated contracts, changed PHP syntax, all 32 Node files and production build passed. A real isolated redirect/removal check and selected-game login/nation-creation journey passed with CSRF, localization and multipart images. The full isolated administration journey retained game, turn, account, inspection and map operations. All 22 representative entry/client/HUD Chromium checks passed after aligning historical selectors and setup URLs with stages 4–5. All database work used a disposable socket-only fixture; live games and accounts were untouched.

## 2026-09-22 — AI conflict escalation experiment

- Added the requested deliberately lively escalation behavior inside disposable V1 only. High-hostility conflicts sometimes open two or three attacks against different territories of the same opponent, using a repeatable seed/turn roll. Each front must independently meet the existing five-Artillery/two-Infantry composition and estimated power threshold; the policy never duplicates a division across orders.
- A bot engaged in conflict now expects retaliation: it holds two to four Infantry out of its assaults, grows toward enough Infantry/Artillery for the planned fronts, and moves reserves toward the contested frontier. Human protection and normal server validation remain unchanged.
- Per user direction, verification was intentionally narrow: PHP syntax and the focused pure-policy test pass, including a high-conflict multi-front attack with retained guards. No full engine/client regression or live-game step was run; the user will playtest the active game.

## 2026-09-22 — Multi-game stage 4: accepted gameplay additions

- Added persistent public owner inspection with leader portrait/flag and public statistics, selected attack/defense totals in World and Military, and confirmed before-battle territorial defense with staying/incoming/deployment contributions. Traced actual engine phases: disband orders still defend, because disbanding happens after combat. No combat-rule changes or enemy-strength guesses.
- Added compact public productivity details using authoritative terrain rates and the population unit in existing territory exports. Shared public nation identities across inspectors, briefing and Reports through the game service's generation-fenced read cache; one closing consumer cannot cancel another's request.
- Added hidden-tab turn favicon attention with acknowledgement, reduced-motion fallback, original-icon restoration and instance cleanup. Existing sounds are unchanged. EN/FR and existing UI tokens/disclosures are used.
- Verification: all 32 Node files, focused cache/force tests, 20 stage-4/persistent-panel/HUD Chromium checks, final focused stage-4 suite and the real multi-game browser journey passed. PHP public-export checks used only the isolated fixture; PHP contracts/syntax, generated-source checks and production build passed. Camera regression now waits for its rendered frame. No live game mutations or migration. Stage 5 was still pending at this checkpoint and is recorded above.

## 2026-09-22 — Multi-game stage 3: destinations and lab directory

- Connected Games, Administration and Tools & experiments with shared native navigation and server-provided links. Added a development-only guest lab directory and return paths from Map Lab, Portrait Lab and UI gallery. No game requests run in the directory.
- Administration login retains its destination; normal login and scoped nation setup retain their flows. Preserved development/authentication boundaries, including existing non-admin administration access and guest lab access. No roles/invitations system.
- Administration keeps game/map/account operations and the scoped object inspector, while removing API-console/generated-service advertisements from the new flow. Backend legacy removal remains stage 5. Added a minimal collapsed first-install command hint; no provisioning operation was executed.
- Verification: real isolated destination/login/lab journey and French narrow layout passed; no-database access/link contracts, PHP syntax, all 31 Node files, generated definitions and production build passed. All six entry regressions and the full isolated game-selector regression passed. No live game/account edits.


## 2026-09-22 — Multi-game stage 2: selector and instance switching

- Delivered `/client` active-game cards with placeholder previews, play/setup/spectator actions, EN/FR and responsive layouts. Unselected new-client login reaches the selector; explicit setup links retain the selected game.
- Replaced the permanent page-level game services with disposable `GameInstance` lifetimes coordinated by `GameApplication`. A fixed transport belongs to each game; retired reads/polling/automation and private snapshots cannot leak into the next game. A visible Games action supports in-page switching, real game URLs, history and independent tabs.
- Guarded pending commands, local drafts and unresolved outcomes. Added per-game route/briefing preferences alongside existing camera/layers/mode storage. Spectator views skip private owner reads even for the owner account. No new permissions system, schema change or legacy cutover.
- Verification: isolated real browser selector/switching/cleanup/history/camera/two-tab journey, pending/draft guards, delayed old response and login/setup passed. All 14 existing entry/live-data and 11 persistent-panel browser regressions plus the narrow French header check passed; 31 Node files, added spectator-data coverage, PHP contracts/syntax, generated check and build passed. Live games were untouched. Stages 3–5 remain pending.


## 2026-09-22 — Multi-game stage 1

- Implemented the first stage of the user-approved five-stage multi-game/legacy-retirement plan. Independent game creation preserves existing active games; no schema migration or live game changes. Added central open-game eligibility and request-local selection with conflicting-ID rejection and a sole-active-game compatibility path.
- Routed public/owner/setup/upkeep contexts through explicit selection; added the account's active-game list. Scoped AI/turn operations and homeland locks, removed creation-lock coupling to existing games, fixed the cached territory game key and required explicit CLI turn targets. Archives remain inspectable by administration and cannot advance through player/AI/engine paths.
- Minimal client integration pins the page game in transport headers and entry/setup links. Existing admin creation API/UI now describes and returns independent games. No selector, navigation redesign, parity additions or legacy deletion was implemented.
- Verification: all 31 Node test files, generated-source/no-database PHP contracts, PHP syntax and production build passed. New isolated MariaDB engine suite covers simultaneous classic/generated games, shared-account nations, failed creation, independent locks/AI/turns/rollback/scheduler/caches/status files and archived data. Real HTTP/Chromium suite covers auth/CSRF-backed commands, spectators, foreign game IDs, multipart creation in both games and two actual game tabs sharing a login. The existing real administration browser regression also passed with independent-game creation expectations. The standalone generator browser journey passed the selected-game redirect, actual homeland wizard and ownership display; its old finder/briefing interactions were updated to the current UI.
- Recorded [ADR 0017](decisions/0017-multiple-active-games.md) and [the maintained multi-game plan](multi-game-plan.md). Existing general human-command rollback revision and same-turn atomicity limits remain. Next stage: game selector and instance lifecycle.

## 2026-09-21 — Attacker, defender and neutral identity in reports

- Combat headlines now persist battle-time attacker, defender, territory and outcome as nullable structured context while retaining safe fallback text. Future headlines are one per battle, avoiding the prior ambiguity where an attacker summary could combine territories from several defenders. Existing historical headlines remain unchanged and readable.
- Turn briefing and Reports share compact identity badges with existing nation flags and confirmed primary-colour accents. Neutral defenders are labelled and use a pirate-flag marker; names remain visible so colour and flags are not the sole signal. Detailed battle summaries now show both participants as well.
- The UI/data skills kept rendering in one shared presentation helper and authoritative participant identity in the battle/news record. No ownership is inferred from the post-conquest map. Added EN/FR briefing labels and focused nation/neutral, refresh, safety and narrow-layout browser coverage.

## 2026-09-21 — World ranking charts and history

- Replaced the five Reports ranking grids with labelled horizontal bar charts using current public ranks, values and confirmed nation colours. Added Current/History tabs inside World rankings; the rest of News, battle, victory and nation reports is unchanged.
- History lazily loads one selected metric as multi-nation SVG lines, with nation filters, dash-pattern identity, missing-observation gaps and an exact semantic table containing values and ranks. Tab/metric/filter/disclosure/focus/scroll state survives same-context report refresh. Narrow layout scrolls the chart internally without widening the page; no chart dependency or generic chart framework was added.
- Added stable ranking keys and a context-fenced public history endpoint. Four grouped queries derive all five metrics from existing per-turn snapshots inside a consistent read transaction. No migration, backfill or persistent ranking-history table. Army and wealth keep their existing public approximation. The service shares/caches the lazy read by game/turn and invalidates it on scope changes or turn transitions.
- Verification: all 31 Node test files, generated-client/no-database contracts, PHP syntax and production build passed. The rankings plus ten-case persistent-panel Chromium run passed; desktop/mobile screenshots were reviewed. A guarded read-only active-game check at turn 502 returned all five metrics, matched each current ranking's final point, and blocked database writes. A separate benchmark returned 20,080 points / about 846 KB in about 436 ms. No live game state was changed.

## 2026-09-21 — AI V1.1 regional strategy

- Upgraded the disposable AI policy after read-only inspection of the active turn-439 game showed viable economies but fragmented campaigns: six of seven targets lay outside the bot's largest land component, cautious nations occupied up to seven components, and several rival pairs had fought dozens of battles. The old turn bonus made all personalities increasingly warlike, while global coastal reach encouraged target hopping.
- Local neutral growth is now normally strongest. V1 derives authoritative land-only components, lazily fixes a home landmass and a stable seeded land-focus/doctrine, and can prefer local unification over an overseas neutral. Cautious regional powers can remain satisfied after controlling home; balanced seeds are continental/frontier; aggressive conquerors may start wider wars. Human protection still wins over personality.
- The adapter exposes only the bot's own recent nation battles. Existing JSON memory now retains bounded per-opponent hostility, last contact and battle/attack counts; attacks/losses weigh most, quiet turns decay scores, and battle IDs prevent recounting. No schema, engine, turn resolver or client asset change. Existing games adopt it on each next uncommitted decision; committed orders are untouched.
- Verification: PHP syntax, expanded pure-policy scenarios, generated-client consistency and no-database PHP contracts pass. A read-only current-game preview loaded all seven real observations: five chose local neutral growth and two retained active conflicts. A final read confirmed turn 439 still had zero ready nations and all bot memory remained V1, proving the previews made no game, order, readiness or memory write. Full isolated engine simulation remains separate from this read-only active-game evidence.

## 2026-09-21 — One overlapping geographic-feature atlas in the Map Lab

- Extended the existing ocean/river atlas with continents, islands, mountain ranges and lakes. A common feature registry, reverse multi-membership cell index and typed relationships keep geographic identity separate from terrain, political ownership and label visibility. [ADR 0016](decisions/0016-geographic-feature-atlas.md) records the user-approved direction and future naming provenance boundary.
- Connected landmasses classify by normalized dry-land area, with a native minimum-area preset; classification preserves feature IDs and physical geography. Elevated connected terrain includes saddles but splits at low valleys. Lake naming uses connected drainage courses and relative contact flow, preserves tributary names, and does not invent outlets for terminal drainage.
- Existing ocean/river names and resource/military/naval options remain. Every new category has an independent display checkbox and lookup entry; selected features receive label priority and footprint tint. The inspector lists overlapping memberships/relationships, and the former Lake Mere placeholder is replaced by the same atlas lake name. No shared game renderer/generator, saved format, backend, dependency or live game change.
- The UI skill guided reuse of native controls, semantic chrome and readable alternatives; no speculative shared widget. Names/IDs remain reproducible for the same sampled map, not persistent across regenerated topology. Forest/plain/tundra regions, archipelagos, peaks, settlement naming, advanced range splitting and curved labels remain deferred.
- Verification: all 31 client Node files passed, followed by the final focused atlas suite with name-collision coverage. Production build and formatting passed. Twenty distinct Chromium map scenarios passed across the atlas/economy/naval/Terrain-v2 and atlas/geography/military runs plus the final five-case rebuilt-assets rerun. One multi-rebuild geography journey initially exceeded its 30-second whole-test budget without an assertion failure; a 60-second budget passed in 29.6 seconds on rerun. Reviewed continent, island, range, lake and narrow-layout screenshots; corrected the cell-inspector placeholder found during review. No physical-device or non-Chromium claim. See [Map Lab](../game-design/map-lab.md) for precise heuristic limits.

## 2026-09-21 — Ready stays available during other participants' activity

- Implemented the approved client-only fix: experimental bot steps now use their own removable `AICommands` lane, not the human GameplayService busy/outcome state. Ready and other human command controls no longer flash disabled for each bot step; bot completion no longer produces human-command feedback sounds/notices. Auto-ready still uses ordinary human Ready.
- GameDataService owns fresh post-activity reconciliation, coalesces notifications and waits around overlapping human preflight/submission/reconciliation. A human command can supersede a background read without losing the AI reconciliation; late data cannot overwrite the newer publication. Own-command single-flight, unknown-outcome review, stale/access/context protection and real turn processing remain gated. Manual commands during a bot step correctly pause Auto-ready.
- The client-data/UI skills kept coordination in services and preserved existing Button/header/map instances; no new widget, data store, engine change, backend lock change, schema or dependency. Updated the living plan, ADR 0010, UI interaction notes and complete-removal inventory.
- Verification: all 31 Node test files passed, followed by the final five-file service regression including overlapping activity, slow AI/human submissions, preflight races, late reads, uncertainty and disposal. Production build, formatting, generated definitions and no-database PHP contracts passed. The 23-case Chromium AI/HUD/commands/live-data/static-status/transition run and final seven-case rebuilt-assets rerun passed; tests directly observe disabled-attribute changes and submit Ready during held AI and human-only refreshes. No live game was changed; browser mutations used intercepted fixture APIs. No new load-test or cross-browser claim.

## 2026-09-21 — AI Player V1Experimental implemented

- Implemented the explicitly approved disposable bot: plain-input policy, one-step runner, normal creation helper and a narrow game adapter. AI settings/memory/results live in three additive tables. Core integrations are participant eligibility/reset hooks plus shared command validation/production allocation; no strategy was added to combat/upkeep.
- Classic/generated creation supports 0–10 bots, mixed/cautious/balanced/aggressive profiles, seed and human protection. A watch-test preset chooses six mixed bots with protection. Browser-owned sequential steps show individual readiness; Auto-ready uses normal player Ready, an adjustable countdown and explicit pause/recovery. Recent attacks trigger limited Infantry defense. Admin exposes preview/step/current-turn batch/stop/pause/resume/release; the bounded CLI never readies humans.
- The client-data skill kept scheduling in a scoped service using existing command reconciliation and confirmed snapshots; the UI skill kept controls as shared Button/FieldShell/Disclosure/ConfirmDialog composition. No parallel game store or new UI framework. The normal map/camera survives AI refresh and watch-mode turn changes.
- Isolated checks cover pure policy, four allocator/forecast cases, atomic failure after Ready, duplicate/stale context, two-process shared-lock concurrency, protection after same-turn ownership change, rollback and controller removal, provisioning rollback and disabled-module human games. Six bots reached turn 63 with 40 neutral and 9 nation battles; ten bots completed three generated-map turns. Node checks, generated PHP contracts and focused Chromium regression pass. Full evidence/limitations and the deletion inventory are in the [handoff](../../modules/ai-player/README.md).
- A compact-game test caught a real pre-command affordability issue: deployment forecasts must not spend Ore that would be produced only after deployment expenses trigger reallocation. Added a batch-wide pre-command resource check and isolated regression. Failure correctly rolled back/paused instead of leaving partial commands.
- That compact fixture reached ordinary victory at turn 48 after explicit recovery from the turn-43 affordability failure; no victory/start-resource rules changed. It was not an uninterrupted from-start run of the final policy. Real HTTP checks also passed for auth/CSRF, archived reports, setup preset/cancel and legacy-shaped AI command rejection.
- Fixed a pre-existing in-memory victory-status type inconsistency exposed by immediately advancing a newly created zero-bot game: both assignments now store the enum's integer value. No victory rules changed.
- Installed only the additive AI migration and rebuilt client assets. No live game/nation was created, changed or advanced; all gameplay mutations used the isolated test database. The experiment remains intended for complete removal, preserving normal nations/accounts/orders/history.

## 2026-09-21 — Minimal, disposable V1Experimental

- User clarified that external means outside game code, with API integration preferred later; explicitly required this experimental AI Player to be completely removed eventually. Added a prominent lifespan/removal section to the [plan](../game-design/ai-nations-plan.md) and revised [ADR 0015](decisions/0015-ai-player-boundary.md).
- Simplified the proposed implementation to V1Experimental, a sequential runner and a small game adapter. Deferred provider registries, formal protocol/memory-version infrastructure, remote transport and broad core refactoring. Retained normal validation and minimal duplicate/stale-step safeguards.
- Recorded the agreed visible sequence: each new turn starts AI work; nations commit actions and become ready one by one while the human plays/watches; everyone ready triggers normal resolution. Proposed one bounded server step per open-client request, with no new daemon/queue requirement.
- Retirement requires deletion of experiment-owned code/wiring/storage after explicit nation handover, preserving ordinary nations/accounts/orders/history. A short implementation removal inventory must distinguish V1-only artifacts from independently useful shared game functionality. Documentation changes only; no implementation or deletion performed.

## 2026-09-21 — AI Player isolation requirement

- User explicitly required the experimental bot to become a replaceable AI Player implementation, mostly outside core game code, allowing possible future LLM-driven players. Recorded accepted direction in [ADR 0015](decisions/0015-ai-player-boundary.md) and revised the [V1 plan](../game-design/ai-nations-plan.md).
- Proposed a dedicated module, `rule-based-v1` strategy, plain versioned observations/decisions/memory, module-owned persistence and a narrow Laravel bridge to shared game-owned reads, forecasting and commands. Core models/resolver must not import concrete strategies or implement personality/campaign policy.
- Corrected the earlier execution design: decisions run outside turn locks/transactions; application reacquires the lock and revalidates context, ownership/control and costs. Future remote latency/failure stays in provider/runtime infrastructure. Added substitution, dependency, delayed-result and module-disabled acceptance checks.
- Preserved 0–10 start-game bots, economic/combat behavior, Auto-ready and protection. Added explicit autonomous-victory versus stalemate acceptance. No LLM SDK, module scaffold, application code, database or live-game changes; documentation/source review only.

## 2026-09-21 — AI nation feature planning

- Recorded the requested viable economic/military bots in [the AI nations plan](../game-design/ai-nations-plan.md), including growth, recruitment, sustained upkeep, grouped neutral attacks, aggressiveness and later nation conflict through shared commands.
- Incorporated the user's setup constraint: configure 0–10 bots when starting a game, normally five or six. Proposed six/mixed when enabled, atomic setup with the old active game preserved on failure, and reserved human starting space. Scale verification now targets six and ten bots; larger counts and mid-game additions are deferred.
- Added the user's watching options: independent player Auto-ready with delay/pause and game-start protection from bot-initiated attacks on human nations. Recorded ordinary readiness/defense semantics, mixed-game consent, viewing limits and protection checks for queued attacks on contested neutral territory. These remain planned, not implemented.
- Source audit corrected the preliminary assumptions: normal five-territory starts cannot immediately recruit seven divisions; 5 Artillery + 2 Infantry gives 180 attack power and a calculated 60% first-attempt neutral conquest probability without partisan support; neutral militia regenerate per battle; exclusive primary colors currently limit total nation slots to 24.
- Proposed staged calibration, shared command/allocator boundaries, allowed-information snapshots, turn-context execution records, rollback/control coordination and bounded admin simulation. Seeded decisions do not make existing random combat deterministic.
- UI/client-data skills guided reuse of admin controls/service ownership and existing gameplay reconciliation. No new component, data contract or accepted architecture decision was implemented.
- Verification: read-only source/document review and local documentation-link checks only. No bot runtime, build, game simulation, database change or live-game action. Opening timelines, policy thresholds and large-game performance remain to be measured during implementation.

## 2026-09-21 — Approved UI polish, rankings deferred

- Implemented primary/secondary choice during creation and review, shared with the existing nation identity editor. Added four secondary-only neutrals via a small migration; retained the 24 exclusive primary slots and legacy callers that omit both colours. No existing nation colours or gameplay rules changed.
- Added a scoped reusable mobile EdgeDrawer for the mode/tool rail, with tap/swipe handle, inert closed content, Escape/outside close and reduced-motion support. Header actions compact to icons/counts while retaining full accessible names.
- Move/attack now previews metadata-based costs, available resources and shortages. Owned movement has no invented fee; newly hostile orders and already-operating units follow the existing server check. Invalid/short-resource drafts cannot submit.
- Rejections show danger treatment immediately and use a distinctive optional sound through saved audio preferences; drafts and existing reconciliation are preserved. No new UI framework or dependency.
- Verification: all 29 Node test files, PHP contracts/generated-client check, focused Chromium journeys, isolated colour constraints and real nation creation with chosen colours. Mobile swipe automation waits for the closing transition before locating the handle. Build refreshed; physical-device gesture and sound feel remain user testing.
- Rankings remains for the next pass. Backend performance redesign remains deferred.

## 2026-09-21 — Static turn-status transport

- User explicitly approved a server-written public file served by Apache, checked every three seconds, to expose processing before the last player's Ready response finishes. `GameTurnStatus` uses atomic same-directory replacement and only public game/turn/state/revision/timestamp fields. Existing turn-lock callbacks wrap advance/rollback; creation publishes after commit. Ordinary readiness reads initialize/repair files under that lock. I/O failures log without failing engine work; exceptions publish failed, not ready.
- The client-data skill guided one service-owned static reader, not a second game-data store. `createTurnStatusReader` omits credentials, bypasses caches and validates payload/scope. Production static polls continue while commands or PHP full reads are pending; the old PHP hint is fallback only. Healthy full refresh remains thirty seconds. Static state gates the existing transition overlay; confirmed reads alone unlock commands. Missing/stale/failed signals recover through HTTP without retrying mutations, and already-reconciled completion does not replay the animation.
- Found the concrete last-player issue: old polling returned immediately whenever a command was pending, while Ready waits for upkeep. The static scheduler removes that blind spot; it does not optimize the actual turn calculations or promise notification before PHP reaches processing. Browser/OS throttling remains outside application control.
- Deployment checks: the real Apache host served a temporary JSON probe directly and returned a static 404 for a missing file, without Laravel session headers. Removed that probe afterwards. The host did not add Cache-Control (the optional mod_headers directive is conditional); the reader always uses no-store plus a timestamp query. Granted only `public/var/turn-status` a `www-data:rwx` ACL because changing its group required unavailable sudo credentials. Existing public-directory permissions are unchanged. Runtime status files are ignored; existing games initialize on their next ordinary readiness read.
- Verification: production build, formatting, generated-client check, PHP no-database contracts, PHP publisher tests and all **28 Node test files** passed. A **16-case Chromium** HUD/live-data/transition regression passed, including holding Ready open until after the overlay appears; final two-case static/transition rerun also passed after late-signal and startup recovery refinements. The real engine advance/rollback test passed on the explicit isolated MariaDB/public root, observing processing before the old turn ended. Stopped that test database afterwards; no live game was advanced, rolled back or otherwise mutated.
- Updated ADR 0010 and the living data plan. File notification revision is not the deferred durable DB context/command fence; broader core performance, WebSockets and game-rule changes remain deferred.

## 2026-09-21 — Experimental turn-loading overlay

- Added shell-owned `TurnTransition`: native blocking modal, gentle backdrop blur, animated N, rotating EN/FR world-building messages, reduced-motion support, and no fake progress percentage or artificial minimum duration. Ordinary same-turn refreshes and Ready submission alone do not trigger it. Existing map/camera/views remain mounted; turn news opens after the overlay closes when no other dialog is holding the existing briefing queue.
- Following the UI/data skills, presentation composes existing Button/tokens/Scope while GameDataService owns a separate `turnTransition` signal and submission gate. Same-scope turn changes or explicit not-ready markers start it; confirmed publication ends it. Access/scope loss clears it. Old marker observations are cancellation/generation guarded.
- After 15 seconds, humorous text becomes an honest waiting message and Retry can replace a stalled read. Read failures stay blocked with readable recovery. Explicit `retryTurnTransition()` cancels only reads, never mutations, and late generations cannot publish. No new backend, dependency, API, WebSocket, resource rule or permission.
- Verification: all 27 Node test files passed, followed by the updated focused live-data tests (including late-read recovery). Production build and formatting passed. The final combined **15-case Chromium** regression passed: blocked keyboard/mouse/Escape, narrow layout, reduced motion, slow/error recovery, Retry, news handover, unchanged canvas identity and existing HUD/live-data workflows. An initial beta-map case exceeded its initial-load assertion under concurrent test load; its standalone rerun and final combined run passed. Tests used isolated fixtures, not live game mutations.
- Limitation: detection begins at the first HTTP observation, not guaranteed server processing start; the existing readiness endpoint can wait on the turn lock. Discussed a server-written public status file served directly by Apache as a potential early signal, but did not implement it without an implementation request. Existing faster polling remains in place. Broader core performance redesign is still deferred.

## 2026-09-21 — UI accepted; faster turn detection and inactive-tab polling

- User accepted the current UI after extensive playtesting: usable at this stage, with future polish still possible. They explicitly deferred the diagnosed command latency/database-query reorganization to broader core discussions. That performance pass is not implemented or silently folded into this change.
- Implemented only the requested client polling adjustment: existing public `/game` hint about every 2 seconds, full confirmed refresh on game/turn changes, and the existing full same-turn refresh about every 30 seconds. Hidden documents remain scheduled. Unchanged hints do not republish owner data or reset its freshness timestamp. Lower turn numbers also trigger refresh, but missed same-number resets still require the deferred durable revision contract.
- Used the client-data skill to keep ownership in GameDataService and reuse full reconciliation. Hints have cancellation/generation guards, a 10-second timeout, stale/access-loss handling and bounded retry backoff. Commands/full reads supersede hint requests; timers/wakeup events are coalesced and scoped. Visibility return, focus, online and restored-page events request prompt refresh. Browser/OS throttling or device sleep cannot be disabled by this code; no keep-awake/audio workaround, new permission or WebSocket.
- Verification: all 27 Node test files, production build, generated-client check and PHP no-database contracts passed. Final focused service/store/gameplay tests and formatting passed. The final combined **24-case Chromium** HUD/live-data/persistent-panel regression passed. New tests exercise two-second hints versus thirty-second heavy reads, hidden scheduling, advance/rollback, changed-game private-state clearing, session loss, late hints after commands, backoff, return-event coalescing and disposal. Browser fixtures verify externally advanced turns while visible and under simulated hidden-document state, preserving actual World/minimap canvases. This is not a guarantee of background timing on physical devices.
- Browser regression initially found a redundant ordinary `pageshow` refresh interrupting initial news and doubling initial owner reads; restricted that wakeup to restored (`persisted`) pages and reran the suite successfully. Existing command uncertainty, stale context, accepted-but-unreadable recovery and persistent player panels remain covered.
- Updated the living data plan and ADR 0010 with the accepted cadence/pause-policy change. No backend/schema/API changes, live game reads/writes or new dependencies; fixture browser server stopped with its test runner. Broader core performance redesign remains deferred. UI polish is accepted rather than awaiting approval.

## 2026-09-20 — Lab-only Terrain v2 visual comparison

- Implemented the user-approved optional close-up terrain experiment, explicitly confined to the Map Lab. The original renderer stays default and the in-game/shared geographic renderer is unchanged. A native toggle preserves the current camera/map; a separate locator focuses a forest/river comparison scene.
- Replaced mirrored material fills in the optional view with seeded continuous ground fields, tree-canopy clusters/clearings, shoreline colour/depth bands, snow/rock treatment, and subdued river banks with bounded corner rounding. Decorative sampling never writes geography, ownership, resource stock, drainage or simulation state. Existing layers and diagnostic views remain independent.
- Reused the existing UI patterns and renderer subclass hooks per the UI skill. Procedural Canvas assets avoid a new bitmap dependency. Visible detail patches are prepared incrementally, with adaptive resolution and a bounded 32 MiB patch-pixel cache; no claim of guaranteed frame rate or final photorealistic mountain art.
- Verification: all 27 client Node test files, the production build, formatting check, and the combined 19-scenario Chromium map regression passed (terrain comparison, atlas, geography, military, economy and naval replay). Reviewed the revised identical-camera comparison, close-up and naval-coast screenshots after increasing forest density and rock definition. Browser checks cover unchanged geography/camera on A/B toggles, independent layers, missing original artwork, 22,200-cell resolution, and cache bounds. Mountain shading remains an initial treatment; no physical-device or non-Chromium claim. Scope and implementation details: [map lab](../game-design/map-lab.md).

## 2026-09-20 — Map-lab atlas and natural-resource experiment

- Added optional geographic names, ocean-membership tint, and natural-resource survey controls to the existing lab; preserved all settled geography controls and existing military/naval/economic layers. No shared generator, backend, saved-map format, live game, or new dependency changes.
- Ocean membership uses connected components, normalized coastal clearance, separated open-water seeds and multi-source weighted sea-distance assignment. Every ocean cell (including ice) belongs to one connected named area; labels anchor inside their own area near its centre. Small partitions use the Sea suffix; nested seas/gulfs and persistent cross-resolution identities remain deferred.
- River identities follow actual drainage, larger-flow branches and lake gaps, with named tributaries and selected-course highlighting. Oil/iron/copper/coal fields use continuous seeded coordinates; timber uses existing forest cover. Abundance/concentration/richness presets change the resource overlay, not geography. Only onshore Oil feeds the economic fixture; its capacity updates preserve stocks and queued costs. Stock quantities are area-normalized illustrative units, not new final mechanics.
- Followed the UI skill: native labelled controls, readable inspector/list alternatives, semantic field tokens and a feature-local composition instead of a speculative shared component. No extra UI framework or bitmap assets. Lab state is separate from shared live-data services.
- Verification: all 26 client Node test files passed, followed by the final focused atlas suite including a synthetic confluence/lake-gap case. The final production build, formatting check, and combined **17-scenario Chromium map regression** passed (atlas, existing geography, formations, economy and naval replay). Screenshots reviewed for ocean placement, resource distribution, top-layer selected-river highlighting, and 430-pixel layout. No live database writes or physical-device/non-Chromium claims. Details and honest limits: [map lab](../game-design/map-lab.md).

## 2026-09-20 — Compact military panels (#3–#5)

- Implemented only the user-approved deployment-card, grouped-pending-order and bounded space-cleanup steps. Economic map/territorial editing (#2), additional whole-economy previews and WebSockets remain deferred. No backend/game-rule changes, live game mutations, new dependencies or generated artwork.
- Deployment artwork now carries separate remaining-affordable and unconfirmed-preview counts. Existing shared `?` help exposes selected-unit costs/statistics and current blockers; short validation retains full hover/focus/tap detail. Draft cost uses existing HUD resource symbols, with exact names/costs/upkeep in help. The preview total expands its placement list, and the native list/quantity alternative uses inline fields. Brush placement, Undo/Clear, mixed drafts and one explicit confirmation are unchanged.
- Pending orders group by action, destination and command kind, with existing unit icons/counts and expandable individual rows. Group cancellation submits the latest matching IDs through the existing batch APIs; individual cancellation remains available. Deployment and active-division ID namespaces never mix. Accepted cancellations no longer remount the already-open Orders panel. Keyed rows/groups retain focus and expansion during ordinary refresh; moved surviving focused rows remain accessible in their new group; removed controls leave the pending-control collection.
- UI/client-data skills guided the small additive `ImageChoice.setBadges` contract and gallery example, existing Tooltip/CompactMessage/Button/Scope reuse, pure grouping projection and feature-local composition. Compact close/actions and removal of redundant rows/nested placement scrolling preserve readable text and normal command targets. The desktop deployment test fits at 1440×900 without zooming or panel scrolling when optional details are closed; mobile remains a scrollable bottom sheet, not a claim that all content fits a phone.
- Verification: all 25 Node test files passed, followed by a final focused five-file service/control regression. Final production build, formatting, generated-client check and PHP no-database contracts passed. All **36 Chromium scenarios** in the final combined military/foundations/live-data/persistent-panel run passed, including five new compact-panel scenarios. Coverage includes shared affordable/preview counts, optional details, current group and single-ID cancellation payloads, mixed unit/action/destination grouping, rejection, uncertain outcomes with no retries, accepted-but-unreadable recovery, stable focus/disclosures/canvases and view-resource cleanup. Existing tests were adapted to expand individual rows before selecting their cancel controls. Reviewed desktop, narrow and French mobile screenshots. Synthetic fixtures only; no new real-database, other-browser or physical-device test claim.

## 2026-09-20 — National production planner

- Implemented the user’s selected next step, leaving the economic-map/territorial-editor concept for their follow-up. World’s Economic button and the Economy page now open a centred dark/brass FeatureSurface dialog instead of mounting the long per-resource production editor. Food/Ore/Oil are compact slider/exact-value rows with current→forecast output, labour and net balance. Existing Material bids are preserved behind a disclosure marked as having no current use; cutoffs and territorial allocations are also disclosures. Shared `?` help and compact messages carry optional explanations.
- Added a pure joint territorial forecast fed by raw owner-bundle allocation inputs. It accounts for automatic upkeep, reserved Capital labour, saved bid order/priorities, territorial pool sharing, facility capacities, inverse cutoffs, raw-unit rounding and the engine’s reserve fallback. Targets are explicitly extra production, not total output or globally interchangeable labour. Forecasts remain non-authoritative; no allocation mechanics changed.
- Added the authenticated/context-fenced/upkeep-guarded `applyProductionPlan` endpoint: exactly one bid per controllable resource, one transaction, one reallocation. Planner batches take a nation-detail row lock; legacy commands/turn races are not globally serialized. Existing per-resource commands remain supported. No schema change or live-game test mutations.
- Followed UI and client-data skills: reused controls/FeatureSurface rather than another modal component, made surface heading IDs unique, retained service-owned turn-scoped drafts, and updated live values in the same nodes. Only accepted unchanged batch drafts clear after successful reconciliation; newer/rejected/uncertain/unrefreshed drafts survive. Closing/reopening preserves drafts. New-turn changes retain nodes but reset obsolete edits with explanation. No mutation retry or extra private-data cache.
- Verification: all 24 Node test files passed; focused forecast/service/gameplay tests, PHP no-DB contracts and generated-client checks passed again on the final source. The final production build and combined **43-scenario Chromium** regression passed (planner, existing client/commands, foundations, identity, live-data and persistent panels). Isolated classic-game tests verified complete-plan validation, rollback after injected mid-bid and mid-allocation failures, and four forecast/real-engine production and per-facility allocation comparisons, repeated successfully on a later turn. Real isolated HTTP/browser checks passed login/CSRF/all four context fences, invalid batch nonmutation, accepted full-plan save/reconciliation and stable canvas. The full isolated gameplay journey also passed bids/drafts/cancellation, deployment/cancellation, turn advancement, movement/disband, reports/history and mobile layout.
- Desktop, real-game and French mobile screenshots reviewed; tightened mobile close-button wrapping and loaded dialog chrome with the shell to avoid unstyled lazy-loading flashes. Seventy-five local documentation links resolved. Temporary PHP/MariaDB fixture services stopped; no live game values changed. Other browsers, physical devices and two-process command stress testing are not claimed.
- Source/contract detail: [ADR 0014](decisions/0014-national-production-planner.md). Remaining: economic map design, territorial editing, durable rollback-context revision, wider concurrent-command guarantees, other browsers and real-device playtesting. Old ProductionPanel sources remain unmounted for historical reference; no parallel active editor was added.

## 2026-09-20 — Shared help and persistent player panels

- User narrowed the work to a shared `?` tooltip, short visible errors with complete optional detail, and completion of the existing store/subscription migration. Reused the UI/data skill contracts; the earlier World implementation had left an explicit shell reopen condition for dedicated pages and inspector closures on territory updates.
- Added Scope-owned `ui/Tooltip.js` and `ui/CompactMessage.js`, demonstrated in the foundation gallery. Help opens on hover/focus or click/tap, uses the native popover top layer to escape panel clipping, follows viewport scrolling, and dismisses on Escape/outside interaction. Errors retain a short visible summary and expose the full text safely without HTML parsing. Production/deployment/selection/order help and nation-colour instructions use the shared component; player notices and validation use compact messages. Existing theme tokens and EN/FR help labels are preserved.
- `GameShell` now replaces player workspaces only for navigation or incompatible scope/geography. Dedicated Economy, Nation, Military and Reports subscribe to the existing confirmed store, updating existing tables/metrics/forms/canvas. Production drafts/focus/scroll, military controls and selection, and report disclosures remain in place. World forces and pending orders use persistent rows keyed by ID. Territory inspectors consume current public/owner values and retain their disclosures, with lost owner details cleared immediately and guarded identity reads kept on demand.
- Healthy polling retains command availability. Starting a command supersedes the poll and still performs its pre-write context check; stale recovery and command reconciliation remain gated. Background report reads also leave the load control usable. Turn changes reset production drafts and military selection through existing context rules; disappeared selected units receive an explanation. No new transport, backend/schema/API, live-game writes or economic-planner changes.
- Verification: all 23 Node test files passed; focused service/gameplay/foundation tests passed again after refinements. A combined 39-scenario Chromium pass covered existing gameplay/identity/foundation regressions and the new persistent-panel/tooltip cases; two further focused Military/Reports checks passed after the final small refinements. Assertions cover actual DOM/canvas identity, updated values, focus/drafts/scroll/disclosures, turn changes, lost ownership, safe full error text, modal Escape ordering, narrow tooltip positioning, stale gating and exclusion of a late pre-command poll. PHP contracts, generated-client check and final production build passed. Desktop/mobile screenshots inspected. Other browsers/physical devices and the durable backend rollback-context revision remain outside this verification.

## 2026-09-20 — Territorial production guidance

- Reworked the existing shared `ProductionPanel` around linked native sliders and exact numeric fields for national output target and minimum facility productivity. Slider movement remains an in-memory draft; only the existing Save action submits through `GameplayService`, with rejection/newer-draft/refresh behavior unchanged.
- Added a feature-local territorial preview from the confirmed owner bundle: estimated labor demand, currently free local labor, current allocation, eligible territory count, facility output ceiling and a collapsible per-territory explanation. Copy now states that the cutoff excludes facilities rather than improving them, target 0 cancels, and food/upkeep/other bids can change the authoritative allocation.
- Normalized inverse-stored productivity for display, removing values such as `2.999994000012`. Extended the existing shared `RangeField` additively with contextual help and label/bounds setters; no second control family or dependency.
- Corrected two owner-read defects required for honest guidance: Capital stock is converted to raw units in `NationDetail::getFreeLabor()`, and territory pools with no non-Capital allocation now use zero rather than SQL `NULL`. A read-only current-game check showed national and summed territorial reassignable labor both at `6,993,168`; no live game values or commands were changed.
- Verification: all 23 Node test files, PHP contracts/syntax, generated-client check and production build passed. Twenty focused Chromium scenarios passed across identity/feedback, shared foundations and core client regressions, including linked slider/exact input, normalized saved productivity, preserved production drafts/canvas, rejection/acceptance, long panel content and 390 px no-overflow coverage. Desktop/narrow screenshots inspected.

## 2026-09-20 — Portrait collection 03 and lens materials

- User requested twelve more faces, twelve garments, colored/dark/mirrored glasses and two or three more of the other options. Added 39 independent transparent masters: 12 faces (six per pool), 12 garments (six per pool), six hairstyles (three per pool), three frame designs, three facial-hair designs and three neckwear designs. Added three palettes and hair-color swatches. Each pool now has nine faces, seven hair choices including bald, twelve garments and six glasses choices; facial hair remains male-only.
- Used built-in image generation, one call per asset; original RGBA outputs and exact prompts/provenance are saved in `resources/js/portrait-lab/assets/collection-03/`. Original atlases/registrations remain unchanged. Additive metadata records source eye/scalp measurements, garment masks/collars and supported pools. Fixed rear-collar tips covering long faces by opt-in head-in-front composition on new garments; corrected the overalls fabric mask after contact-sheet review.
- Lens finish is a frame property: clear, color tinted, dark/black or mirrored, plus tint color. Source-space contours follow the same fitted transform as the frame. V4 explicitly saves finish/color, retains them across pool/garment changes and rejects invalid values; v1–3 remain supported, with clear v3 pixels unchanged. No automatic saved-shelf migration.
- The game-UI skill kept the new controls feature-local compositions of native selects/color inputs and existing FieldShell/Scope. No shared widget, backend/API, dependency, permission or game-data change. Default population sampling includes all 18 faces; original weighted profiles remain selectable. Complete inventory, authoring instructions and source map are in [portrait lab](../game-design/portrait-lab.md).
- Verification: production build, generated-client check, three portrait Node suites and all **12 portrait Chromium browser journeys** passed on the final build. Tests cover 39 RGBA files, pool counts/exclusion, all-face/frame eye mappings, finite compatible placements, 1,000 seeded samples, v4 round-trips, legacy definitions and lens validation. Browser checks cover every new face/garment/hair asset, finishes, pool/restore persistence, old pixel hashes, custom art, shelf errors, mobile layout and face pixels unaffected by collar/lens composition. Reviewed the final 12-character contact sheet and accessory close-up. The first flat collar mask changed face pixels/neck edges; replaced it with a jaw-shaped exclusion and reran the full suite. Actual development route returned HTTP 200; 80 documentation links resolved. Master delivery is about 45 MB; lazy loading/production derivatives, fine contour polish and physical-device/other-browser review remain open.

## 2026-09-20 — Nation identity, feedback and production polish

- Implemented the approved experimental [game UI pass 4](game-ui-pass-4.md) and [ADR 0013](decisions/0013-nation-identity-feedback.md): stronger national exterior bands, quieter internal boundaries, independent generated-map rivers above filled highlights, coherent authored action/resource icons and slightly larger global typography. National paths refresh in both main map and minimap without losing geographic caches. Rounded segment caps fixed dotted-looking edges found in screenshot review.
- Added 24 named backend colours with per-game primary reservation and reusable secondary. Existing nations are backfilled; creation reserves within the same transaction, and capacity failure rolls back creation. Game-row locking plus database uniqueness guard reservations. The menu editor uses native named swatches, local preview and the existing authenticated/context-fenced command service; confirmed colours drive small unit markings. Sidebar palette removed, flat/miniature display choice moved to the menu. Existing art masters, rules and resource semantics are unchanged.
- Added optional synthesised input/unit/command/readiness/turn cues. Silent by default, independent categories, exact volume and browser-local persistence; ordinary refreshes do not replay turn sounds. Economic map mode now exposes the existing national bids and selected own-territory facility facts through the persistent ProductionPanel also composed by Economy. Map-side inputs survive refresh and selection; dedicated-module remount limitations remain explicit.
- Used the game-UI and client-data skills: narrow PaletteField contract, reuse of RangeField/Button/Scope, feature-local production/identity styles, confirmed colours in the existing shared bundle, separate local drafts/preferences, normal reconciliation and canvas/input identity checks. Updated the foundation catalog, live-data plan and index. No new dependency, permission system, universal binding framework or skill-rule change.
- Verification: production build, generated endpoint check, PHP contracts/syntax, all 22 Node test files and 49 distinct Chromium scenarios passed across full/focused runs (client, entry, commands, HUD, unit control, orientation, foundations, live data and four new identity/feedback scenarios). Final 12-case command/HUD/identity rerun passed. New coverage includes reserved primary/reused secondary/local preview, French mobile editor, remembered silence/category controls, positive readiness/turn cues with no replay, persistent production input/canvas, rejection/acceptance and border pixels with detail-independent rivers. Test fixes waited for the first canvas before recording identity, targeted Language instead of every select, and dismissed the fixture's deliberately re-seeded turn briefing after reload.
- Real disposable classic and beta game journeys passed existing production/cancel/drafts, deployment/cancel, readiness/turn activation, movement/cancel/disband, battle/history and mobile layout. Both formats passed API authentication/CSRF/all-four-context-field checks including colour commands and unchanged budget. Isolated database tests passed 24-colour catalogue/backfill, application/database uniqueness, invalid IDs, secondary/cross-game reuse and exhausted-capacity rollback. No two-process stress-test claim.
- Additive identity migration applied to this installation after isolated checks. No live economy, unit, order or turn mutations for testing. Source recovery archive `/tmp/no7-ui-identity-xjLtVy/source-before.tar.gz` is not a full database backup. Temporary PHP and MariaDB fixture services stopped; live database left running. Checked 71 local links in the updated maintained docs. Desktop/narrow screenshots inspected; physical devices, other browsers, multi-device audio listening and colour-vision review remain open.
- Next: in-game playtesting of the provisional icon/sound/colour treatment and production layout. Durable rollback/reset revision and full D3 module/inspector persistence remain separate work, not silently included here.

## 2026-09-20 — Experimental map orientation controls

- Added rotate-left/right (15° per click) and angle/reset-to-north buttons beside World's zoom controls, with EN/FR accessible labels, native keyboard activation and narrow-screen wrapping. Fit world retains orientation; rotation is a saved per-user/game camera preference, with old camera snapshots defaulting to north-up. No shortcuts, tilt, backend changes or new game mechanics.
- The UI skill guided composition from existing buttons and the shared camera rather than a second map widget. `ui/map/Camera.js` now supplies rotation-aware forward/inverse/drawing transforms, screen-relative pan and four-corner viewport bounds. Both classic and beta renderers use them, beta culling includes all four corners, territory text and military sprites remain upright, and the minimap stays north-up with a rotated viewport polygon. Existing entry/admin/map-lab cameras remain unrotated by default.
- Verification: production build, generated-client check, PHP no-database contracts, formatting and all 21 Node test files passed. Camera tests cover rotation, high-DPI drawing matrices, picking, anchored zoom, pan, fit, wraparound and legacy/corrupt preferences. All 28 existing client/entry/HUD/unit browser scenarios passed, including beta-map rotated selection and all five unit types. A new test initially expected an unchecked row that the selected-only list intentionally removes; corrected it to assert the one-unit selection. The final 17-scenario Chromium run passed both high-DPI orientation tests, all eight map-lab checks and all seven live-data checks. Reviewed desktop classic/beta detail/overview and French mobile screenshots. Fixture data only; no live game commands or physical-device/other-browser claim.

## 2026-09-20 — Zoom-aware unit control and deployment previews

- User authorized the first experimental control pass, explicitly requested right-button drag to pan (not Space), and added recolourable units following the portrait palette approach. Implemented only in the new client, with no backend/rule/schema/permission changes. The UI and client-data skills guided small map interaction contracts, feature-local composition, shared confirmed reads and service-owned draft acceptance cleanup; imagegen supplied five transparent sprite masters with prompts retained in the project.
- Military supports count-to-miniature detail, shared drawing/picking slots, left-drag rectangle selection, Shift selection, right-drag pan, and the existing Forces checkbox alternative. Slots are presentation, not authoritative microcell positions. Crowded/small regions remain grouped. Aircraft have separately drawn offset shadows. Appearance switches miniatures/flat counters and local paint/uniform/aircraft-trim colours through cached luminance ramps. Original catalogue art and portrait code are unchanged; material masks remain provisional keyed ranges.
- Deployment is a mixed-type/mixed-territory local ghost draft with shared-budget preview, Undo/Clear and one existing batch submission. Native list/quantity placement remains available. No request is sent for placement clicks. The service removes only accepted submitted local IDs, even after leaving World; newer placements and rejected/uncertain drafts survive. Ordinary data reconciliation keeps the map, camera and focused controls alive. Existing uncertain-outcome and rollback-context limitations remain documented, not silently upgraded.
- Verification: 20 existing Node test files passed plus the new unit-control contracts and focused service/store reruns. Existing 37 Chromium journeys passed after helper adaptation; all six new unit-control browser scenarios passed, including generated land slots, aircraft, palette/style changes, box selection, draft failures, in-flight edits and navigation. Corrected incomplete economy fixture data exposed by the new navigation test. Desktop/classic/beta/mobile screenshots reviewed. Build/generated definitions/PHP no-DB contracts passed. No physical-device or other-browser claim.
- Real isolated classic game 34 and beta game 35 passed mixed deployments across two territories, exact shared Capital/Ore costs, next-turn activation, and persistent canvas. Existing classic command journey in game 36 passed deployment/cancellation, map picking, movement/cancellation, disband/cancellation and mobile. Tests use only the separate temporary database and localhost, never live games.
- Handoff: [pass 3](game-ui-pass-3.md), [ADR 0012](decisions/0012-unit-control.md), updated shared-data plan and UI catalog. Recovery source archive: `/tmp/no7-unit-control-GvCdWR/source-before.tar.gz` (client/tests/docs only). Artwork source masters currently total about 5.3 MB; delivery optimisation and exact authored material masks remain future polish, not claimed complete.
- Next: playtest miniature scale/readability, palette boundaries and crowded-stack behaviour. This is revisable UI experimentation, not a frozen military system or new game mechanics.
- Final real regression also passed the full World-command journey on beta game 37. Temporary PHP and database fixture services were stopped after verification. Checked 114 local documentation links with no missing targets.
- Final production-build regression: all 43 Chromium scenarios passed in one run (37 existing plus six new); generated-client and PHP contracts and focused Node service/draft checks also passed. Fixture browser server stopped automatically.

## 2026-09-20 — Compact game HUD and turn briefing

- User authorized the agreed second UI pass. Added a compact in-game N/menu/turn bar, resource net balances and reserves with exact expandable breakdowns, player readiness, direct one-click Ready and manually reopenable News. Removed classic-interface links from the game header/footer/error state; entry/admin are unchanged. Modules/settings remain accessible through the hamburger menu.
- Moved existing layer toggles into a World-owned header disclosure labelled by current mode. Added a persisted minimap-collapse preference that hides the same overview canvas, preserves the main camera and frees contextual-panel space. Shared `Disclosure` supplies native grouped expansion, keyboard/Escape focus return and outside dismissal; resource symbols reuse bundled assets. UI and client-data skills guided presentation/service ownership and persistent updates; no new skill rules were needed.
- Added the private typed `NationTurnSummary` read model through the existing owner response, with actual previous-turn population/territory comparisons and noncancelled completed deployment counts. No schema, game-rule, permission or command-endpoint changes. Current affordable unit maximums remain clearly separate from completed units. Briefing reports reuse existing service reads, current-result-turn participant battles, public news and text-safe identity resolution. Only the seen identity marker is saved, not report contents.
- Verification: all 20 Node test files passed across the full existing suite and new/final focused tests; production builds, generated-client check and PHP syntax/no-DB contracts passed. 37 distinct Chromium scenarios passed across the 35-case regression run and final six-case HUD runs. Coverage includes direct Ready, once-per-turn opening/reopening, same canvas/input identity, exact/negative/missing resource values, stale feedback, late/aborted reports, private-state clearing, subscription cleanup, EN/FR, keyboard and narrow layout. Desktop/mobile and real-turn screenshots inspected; corrected minimap-toggle wrapping found in the narrow visual review.
- Real isolated classic (game 30) and beta (game 31) HUD journeys passed exact population delta, first-turn null baseline, deployment/cancellation, excluded cancelled production, completed-unit count, direct turn advancement, stable map and seen/collapse persistence. Beta journey also checked menu-to-Economy and return. Existing security guards passed both formats. Complete classic gameplay journey (game 32) passed bids/drafts/deployment/movement/disband, turn/battle/report/history and mobile checks; beta World-command journey (game 33) passed the existing map/picking/movement/disband checks. Updated their helpers for menu navigation and briefing dismissal.
- No live game/account mutations. Fixtures used only `/tmp/no7-entry-db-bBlrtP` and localhost. Source recovery archive: `/tmp/no7-hud-pass-ZKKVld/source-before.tar.gz` covers client/test/docs plus the two changed existing PHP files, not a full application/database backup. Local documentation link check: 114 targets across 27 client documents, none missing. See [pass 2](game-ui-pass-2.md) and [ADR 0011](decisions/0011-compact-game-hud.md).
- Temporary PHP/MariaDB fixture services were stopped after the checks. The final formatting-only rebuild passed with the same runtime asset hashes as the last tested build.
- Remaining: real-device/other-browser verification, newspaper art and future diplomacy/war/alliance indicators. Existing durable rollback/reset context and full dedicated-module/inspector migration remain separate checkpoints. Next is in-game playtesting and refinement, not a frozen final HUD design.

## 2026-09-20 — Client-data maintenance skill

- User approved a focused skill for responsible use and evolution of the shared-data foundation. Added [novus-client-data](../../skills/novus-client-data/SKILL.md) with narrow discovery, service ownership, scoped subscriptions, persistent views, private-state clearing, honest command outcomes and no automatic mutation retries.
- Kept actual APIs and delivery status in the living plan. The skill requires evidence before new abstractions and review for unapproved breaking/backend/storage changes; it does not freeze internal names, authorize deferred migration or claim rollback/concurrency guarantees that do not exist.
- Cross-linked the UI skill, foundation catalog, live-data plan and documentation index. Project source is registered through `/home/chad/.codex/skills/novus-client-data` as a symlink, matching the existing UI skill and avoiding a second maintained copy. Normal invocation policy remains enabled by default.
- Used skill-creator guidance for focused instructions/validation, game-UI guidance for the companion boundary and official OpenAI documentation for skill-discovery reference. Both skills pass the bundled validator; local documentation references and registration target checked. No new scripts, application/backend/build/dependency changes, services or game-data mutations. Runtime tests were not rerun for this documentation-only change.
- Next: apply the skill during relevant client-data work and refine it from actual use. Existing D3 migration and backend context-revision checkpoints remain unchanged.

## 2026-09-20 — Shared-data foundation and persistent World slice

- User approved the plan. Delivered the agreed D0 audit / D1–D2 first pass, keeping the dedicated-module/inspector migration and backend revision checkpoint explicit rather than claiming whole-client completion. Updated [the living plan](live-data-plan.md), [ADR 0010](decisions/0010-shared-game-data.md), index, gameplay notes and foundation catalog.
- Added a small immutable observable store with read-only consumer facade, owned subscriptions, isolated subscriber errors and no nested publication. `GameDataService` now owns the shared world/owner payload, refresh state, coalesced reads, late-response guards, privacy clearing, full same-turn polling/focus refresh and bounded backoff. `WorldService` is only a compatibility export alias.
- World keeps its feature, main canvas and minimap through command reconciliation and turn updates. Resource nodes, unit affordability cards and deployment/move drafts update in place; quantity/focus survives refresh, and newer edits survive a pending command. Both map formats update picking/ownership; beta political paths refresh without discarding terrain/landscape caches. Old disband confirmations retain their original command context.
- Commands now distinguish accepted-and-refreshed from accepted-but-unrefreshed and uncertain outcomes. No mutation retries; uncertainty requires deliberate review before further commands. Stale display is retained with writes gated. Shared controls remain presentation-only under the game-UI skill; no new framework or skill-rule change.
- Validation: production build, generated endpoint check, PHP no-DB contracts, all 19 Node test files, and 31 distinct Chromium world/command/live-data/entry/foundation cases passed across full and focused reruns. New tests cover shared publication/read counts, scope loss, disposal, response races, same-turn changes, input/canvas identity, beta ownership caches, failed refresh after acceptance, uncertain outcomes, preserved newer drafts and stale confirmations. Final focused live-data browser rerun passed all seven cases after correcting a test that accidentally matched the nation count instead of the turn label.
- Real isolated classic and beta World journeys passed deployment/max reservation/cancellation, turn activation, actual map picking, movement/cancel and disband/cancel with explicit canvas identity checks. Existing dedicated-module journeys and authentication/CSRF/context/unaﬀordable-command guards passed on both map formats. Desktop/narrow beta screenshots inspected. Physical-device and other-browser checks remain open.
- No backend, schema, dependency, permission or resource-rule changes; no live game test mutations. Fixture games used only `/tmp/no7-entry-db-bBlrtP` and localhost. Temporary PHP/MariaDB services were stopped after integration checks. Client/test/docs recovery archive: `/tmp/no7-live-data-61SMHY/source-before.tar.gz`; this is not a full application/database backup. Local link check: 102 references across 25 planning documents, none missing. Final store/service checks and production build passed after the unsubscribe-during-delivery cleanup.
- Next checkpoint: playtest the persistent World slice, review the proposed durable game turn-context revision before applying backend/storage changes, then finish D3 module/inspector updates. Current game+turn-number guards still cannot detect every missed rollback/reset cycle; client batching is not server transaction safety.

## 2026-09-20 — Shared-data foundation implementation plan

- User accepted the small store/service direction and requested a careful implementation plan before runtime work. Added [the staged plan](live-data-plan.md) and [ADR 0010](decisions/0010-shared-game-data.md), distinguishing accepted direction from proposed contracts and implementation authorization.
- Inspected command reconciliation, world refresh/polling, shell/feature lifetime, Scope/Signal, map/minimap derived state, transport errors, owner reads, command context checks and rollback behavior. Confirmed refresh-as-remount coupling, same-turn polling gaps and snapshot-reference guards that need migration.
- Proposed one shared current-game bundle, coordinated publication and scoped subscriptions; preserve confirmed data during recoverable read failures, keep drafts separate and handle command acceptance versus refresh failure honestly. D0–D2 prove deployment/cancellation before the full workflow migration. No new state library, WebSocket implementation, permissions or resource-rule changes.
- Recorded the missing reliable rollback/reset context token and limited server read/concurrency guarantees as explicit backend-contract checkpoints, not client-side guarantees. Any additive backend/storage work requires its scope reviewed before implementation.
- Used the game-UI skill's service/presentation boundaries, narrow reuse and planning-only rules. Updated the index and foundation catalog with proposed status; no skill-rule change or runtime implementation.
- Verification: source review and 99 local Markdown links checked across all 25 client-planning documents; no missing targets. No runtime tests claimed. No runtime/backend/build/dependency/service/game-data changes. Next: review and authorize the first implementation pass.

## 2026-09-20 — First map-first GAME UI pass implemented

- User authorized the agreed plan. `/client` World now has a left Geopolitical/Military/Economic mode rail, persistent top-right minimap, contextual selection/tool controls below it, and an on-demand territory finder replacing the permanent directory. The map bounds/camera stay stable when context panels open; mobile context becomes a lower sheet.
- Military mode selects own units/stacks directly on both original and beta maps. Added five thumbnail choices, server-derived affordable maximums, eligible-destination highlights, quantity/cost preview and explicit deployment; existing movement/attack preview, cancellation, pending deployments/orders and confirmed disband actions are available from the map. Module tabs and the classic SPA remain intact. Economic/geopolitical modes initially reuse existing territory information and detailed module links.
- Followed the game-UI skill's narrow reuse boundaries: existing Button/Panel/FieldShell/ConfirmDialog/Scope and territory inspector, new focused ImageChoice and Minimap, and feature-owned WorldCommands composition. The synthetic gallery demonstrates image-choice states. Original art masters are unchanged; five 256px WebP variants total about 36 KB, with live EN/FR labels/counts/state chrome. No new shared framework or skill-rule changes.
- Backend addition is read-only: typed `NationDetail::getMaximumAffordableDeployment` uses existing available production and unit costs; the private gameplay read exports per-type limits. Shared move drafting serves both World and dedicated Military. Existing resources, units, movement/combat rules, permissions, mutation endpoints and command-context protections are unchanged; no schema migration.
- Verification: production build, PHP syntax/contracts, generated endpoint check, all 17 Node test files, and 24 world/command/entry/foundation Chromium browser tests passed. Real isolated classic and beta journeys passed deployment/max reduction/cancel/max recovery, turn activation, actual map unit picking, move/cancel and disband/cancel. Existing gameplay and authentication/CSRF/stale-command guards also passed. Desktop/mobile art and map screenshots inspected; 43 documentation links resolve. Real-device/other-browser review and fuller economic/geopolitical workflows remain open.
- No live game/account/resource mutations were used for testing. Disposable fixtures used only `/tmp/no7-entry-db-bBlrtP` and localhost test servers; temporary PHP/MariaDB services were stopped afterward. A final focused command-browser rerun passed after the selection-highlight cleanup. See [game UI pass 1](game-ui-pass-1.md) and [ADR 0009](decisions/0009-map-first-gameplay.md) for usage, source boundaries, reproduction and limits.

## 2026-09-20 — Map-first game UI plan and unit thumbnails

- User agreed to mode-based main-map controls, direct military unit selection, a contextual right panel hidden without selection/tool, and a persistent top-right minimap. Requested a quick first-pass job list and thumbnails limited to existing unit types.
- Verified Infantry, Armored, Artillery, Fighter and Bomber in `app/Domain/DivisionType.php`. Generated five separate provisional illustrations with the built-in image tool and saved masters/full prompts under `resources/js/client/assets/units/v1/`. Labels, prices and selection states are not baked into the images; equipment era is an art experiment, not a game-rules commitment.
- Added `game-ui-pass-1.md` with reuse-first component boundaries, ordered implementation jobs, military finish line, checks and exclusions. Skill guidance keeps existing panels/buttons/fields/map mechanics in scope instead of inventing a parallel UI kit. No skill-rule changes were needed.
- Artwork visually inspected at source size; delivery-size optimization and card/browser/runtime tests await implementation. No application/backend changes, builds, dependencies, services or live game mutations performed. The UI implementation remains planned, not delivered.

## 2026-09-20 — Portrait fitting and separate character pools

- User approved a consistency pass, specifically separate male/female accessories and fittings. Added independent pool-prefixed bindings, curated hair pools, male-only facial hair and face/collar landmark placement in feature-local `fitting.js`. Shared painted sources can have separate calibration; no cross-pool IDs are accepted in fitted imports.
- Repaired the four glasses fronts using new transparent artwork; original atlases and rendering remain unchanged. Neckwear now follows garment collar anchors and scale: ascot on open blazer, all four styles on the buttoned suit, none on other garments. Hair follows scalp references; facial hair follows nose/mouth/chin/jaw measurements.
- Added pool selection, compatible-part filtering, selectable fit-comparison cards at large/roster sizes and optional labelled/numeric landmarks. Reused existing game UI foundations, native controls and Scope; no new dependency, backend, upload endpoint or game-data writes.
- New definitions use v3 / `clean-digital-fit-v1`. Legacy v1/v2 portraits retain their original renderer and shelf entries; an explicit **Use fitted model** action creates an editable copy. The authoring recipe remains provisional instructions, distinct from saved-character JSON. See [ADR 0008](decisions/0008-portrait-fitting.md) and [portrait lab](../game-design/portrait-lab.md).
- Verification: production build, generated-endpoint check, both portrait Node suites and all nine portrait Chromium browser journeys passed. Checked 500 seeded definitions, cross-pool rejection, all face/frame landmark mappings, compatible comparisons, clean exports with guides active, original pixel hashes and opt-in legacy conversion/shelf preservation. Visually inspected updated Mei/Oskar portraits, beard/ascot combination and comparison view. Hair edges, beard contours, clothing silhouettes and masks remain art polish; other browsers/physical phones and arbitrary future assets are not validated.

## 2026-09-20 — Portrait collection 02

- User requested three additional faces, hairstyles and options in every existing category. Added three faces, three hair silhouettes, three garments, three variants each of glasses/facial hair/neckwear, three palettes, three hair-color swatches and three illustrative population presets.
- Generated two new transparent atlases with the built-in image tool and saved them under `resources/js/portrait-lab/assets/`, with full prompts/provenance in `collection-02.md`. Preserved the original atlas and all original placements/ramp behavior.
- Reused shared FieldShell/Scope for accessory style selects. Extended catalog-driven registration to multiple sheets and garment-specific neckwear positions. Version 2 recipes store optional explicit accessory style IDs; original version 1 recipes remain accepted without adding fields. Older readers reject v2 rather than silently replacing its accessory choices. No new dependency, route, backend or game write.
- Verification: production build and portrait Node checks passed; all seven portrait browser journeys passed. Captured three original portrait pixel signatures before implementation and verified exact matches afterward. Exercised each new face/hair/garment and all four variants of each accessory, v2 restore, v1 imports, storage errors, custom image round-trip and narrow layout. Inspected the three new character previews and full-beard/ascot combination. Remaining accessory fit/edge polish is still an art-lab refinement.
- See [portrait lab](../game-design/portrait-lab.md) for the updated inventory, limits and source map.

## 2026-09-20 — Portrait laboratory proof of concept

- User approved a small lab analogous to the map lab after selecting the clean digital painted style and agreeing to modular portraits, palettes, population defaults, versioned recipes and custom-image support.
- Added development-only guest `/dev-panel/portrait-lab`, a separate Vite entry and links in the old development panel and new admin Developer tools. No game APIs, server uploads, migrations, game data writes, dependencies or permission changes.
- Generated and bundled one transparent painted atlas: three face bases, three hairstyles, three garments and glasses/mustache/tie overlays. Measured source crops/canonical registration compensate for the generated sheet's imperfect layout. Added neck coverage for open collars and material-specific luminance ramps with neutral/accent masks.
- Built live editing, role clothing, palettes/custom colors, roster preview, layer inspection, seed/profile-based six-character sampling, an explicit browser-local shelf, validated portable JSON recipes and PNG export. Custom images are locally decoded, normalized, cropped and embedded in their recipes; generated controls are disabled for custom portraits.
- Reused the game-UI skill's Button/Panel/FieldShell/RangeField/Scope and semantic theme tokens. No new shared component or visual-language fork. Documented immutable-library requirements, authoring standards, small-library limits and the original built-in image generation prompt in the asset README.
- Verification passed on the final build: production build, generated-endpoint check, PHP no-DB route contracts and syntax, portrait/foundation Node checks, and ten Chromium browser journeys (five portrait and five existing foundation checks). Coverage includes exact saved pixel restoration, custom-image round-trip, invalid import preservation, storage quota/corruption preservation, keyboard use and narrow layout with long names. Inspected desktop/mobile screenshots; fixed the open-collar neck gap and refined the uniform trim masks. Actual development URL returned HTTP 200. No live game test commands were sent.
- Remaining limits: three authored face bases with fixed age/complexion, prototype art masks, English lab UI and Chromium/emulated-phone verification. The large asset library, independent facial/aging controls, actual nation presets and gameplay integration remain future work. See [portrait lab](../game-design/portrait-lab.md) for the full handoff.


## 2026-09-19 — Administration replacement and saved-map workspace

- User authorized full original-admin capability parity plus integrated map preparation, explicitly retaining existing access checks without building a new permissions system. Recorded ADR 0007; the replacement remains experimental and the original admin remains available.
- Added `/client/admin`: working-game selector above Overview, Map workspace, Accounts and Developer tools. Includes status/nation cards, original/generated world view, force-next-turn/rollback, classic-game creation, global user/password/session tools, scoped division/deployment inspection and links to the existing developer service-code/API test pages.
- Added explicit game/turn request targeting, archived read-only inspection, target/consequence confirmations, no-store responses and no automatic command retries. Existing Laravel engine methods remain authoritative; only optional expected-state checks/creation-lock coordination were added. No new role system or resource/movement/combat rules.
- Added immutable named map snapshots, saved independently of game creation, with exact reload and start-from-saved-map. Shared MapStudio now serves admin and standalone generation, preserving generator tuning and browser preset keys. In-page drafts survive module navigation. Live maps are not edited; terrain painting/import/library deletion remain deferred.
- Used the game-UI skill's reuse-first boundaries; introduced reusable native RangeField, ConfirmDialog and DataTable for concrete consumers and added synthetic gallery coverage. Existing skill rules needed no change; inventory, usage and boundaries are maintained in the living docs.
- Verification passed: production build, PHP syntax/contracts, generated endpoint check, 14 Node test files, 21 entry/world/gallery Chromium tests, complete isolated admin/browser workflow (including classic/generated creation, force/rollback, accounts/password/session, map persistence and mobile layout), scoped inspector/stale/concurrent-operation contracts, standalone map-to-nation regression and real beta gameplay loop. Inspected desktop overview/map workspace and mobile screenshots; 37 documentation links resolved.
- Installed only migration `2026_09_19_230000_create_map_drafts_table.php` in the development app: empty map library, no game seed or reset. Active game remained ID 5, turn 1 across the migration; actual-host `/client/admin` gives the expected guest login redirect. No live game/account test mutations were performed.
- Temporary isolated PHP/MariaDB services stopped after tests; test data and screenshots retained under `/tmp`. See [admin-experiment.md](admin-experiment.md) for capability checklist, source map, reproduction, existing-access caveats and deferred work.

## 2026-09-19 — First shared UI foundation pass implemented

- User approved the small foundation-first pass, keeping future components and visual direction revisable. Used the `novus-game-ui` skill to keep shared presentation separate from feature state/commands; its existing decision rules needed no change.
- Added semantic aliases in `_tokens.scss` and shared `_foundations.scss`. Added Button/actionLink, Panel, StatusBadge, MetricCard/CardStrip and FieldShell; preserved DOM-returning/localized compatibility helpers and the existing FormField/GlassPanel adapters.
- Entry login/navigation/forms and gameplay panels, fields, readiness, summary cards and command/report buttons now consume those foundations. No resource/rule changes, new dependencies, schema changes or classic-SPA migration. The only route addition is a development-only synthetic gallery at `/dev-panel/ui-foundations`, linked from the dev panel; no game services or commands in the gallery.
- Fixed specificity conflicts discovered in visual/state review (command colours, invalid/read-only fields) and mobile header compression. Kept native form validity, pending versus business-disabled state, labelled status and feature-owned listeners/commands.
- Verification: production build, generated endpoint check, PHP contracts/syntax, 13 existing Node test files plus the new foundation state-contract test passed; all 20 entry/world/gallery Chromium browser checks passed on the final build. Generated-map and original-map gameplay journeys passed against a separate test database, including real bids, deployments, orders, turns and reports. Inspected desktop gallery and mobile gameplay screenshots. Read-only actual-host gallery returned HTTP 200; no live game test commands were sent.
- Updated the living catalog with actual paths/consumers, wrapper contracts, migration limits and reproduction commands. Admin composition, richer controls, full specialized-control migration and real-device/cross-browser review remain later work; this is not a frozen UI library.
- Temporary PHP/MariaDB test services stopped after verification; isolated data/screenshots retained under `/tmp` for investigation.

## 2026-09-19 — Reusable game UI foundation plan and skill

- User agreed to shared semantic variables, reusable UI concepts and assets across gameplay/administration, and requested a quick staged path plus documentation and a maintained skill.
- Added `ui-foundations.md`: current inventory, incremental extraction stages, theme/domain-color separation, reuse/extend/new-component criteria and deferred range/progress/meter/mini-chart contracts.
- Added ADR 0006 for accepted principles; implementation order, candidate names and APIs remain proposed and revisable. Added project-owned `skills/novus-game-ui` with narrow discovery scope and maintenance rules.
- Validated the skill with the bundled validator and checked 28 local documentation links. Registered the project source through `/home/chad/.codex/skills/novus-game-ui` as a symlink, retaining one maintained copy; automatic selection uses its project-specific description. New sessions can discover it as `$novus-game-ui`.
- Updated the index to reflect the already-delivered gameplay/map experiments rather than the earlier read-only phase. No runtime UI, CSS, dependency, backend or game-data changes in this planning task.

## 2026-09-19 — Best-effort current-game port

- Authorized unattended implementation of the current gameplay concept into the new interface; still an experiment, not a frozen future plan.
- Added Nation, Economy, Military and Reports workspaces alongside World. Reused real production bids, deployment/order APIs and readiness/turn advancement. Added shared military map overlays and topology-based movement previews for both map formats.
- Added scoped owner/identity reads and optional server-side command identity fencing in the existing Laravel style. Corrected the budget read model's turn-number field; kept resource, movement and combat calculations unchanged.
- Commands reconcile without automatic retry. Bid drafts survive same-turn navigation; reports preserve participant-only battle visibility and render news safely as text.
- Verification passed: 13 Node test files, PHP contracts/syntax, generation check/build, 16 existing client/entry browser regressions, real gameplay journeys on isolated beta/original maps, actual turn and battle resolution, and authentication/CSRF/context/resource-rejection guards. Live game remained ID 2, turn 1; no live test orders or nations.
- See [gameplay-experiment.md](gameplay-experiment.md) for usage, test reproduction and remaining presentation/localization/concurrency limits. Earlier entries below describe historical scope, not the current delivery status.

## 2026-09-19 — Generated-map beta experiment

- Added a reusable geographic generator/renderer and a new-interface game-creation preview with the lab's existing tuning controls, browser presets and transfer of applied lab settings.
- Beta games save their exact geography and generator settings; the new world and homeland views share hex picking and rendering. Existing-map games retain their rectangular view. The classic SPA is not the target.
- Added typed PHP import/read models, per-game map storage, authoritative territory/connection derivation, transactional creation and stale-start protection in the existing backend style.
- Resource rules, movement budgets and regional combat remain unchanged. Weighted movement and microcell gameplay are deferred.
- This is explicitly a revisable experiment, not an accepted final architecture or roadmap. See [the beta notes](../game-design/map-beta.md) for usage, limitations and isolated verification helpers.
- Verification passed: build, generated endpoint check, PHP contracts/syntax, existing and new Node suites, 30 distinct browser regression journeys, the real isolated generation-to-nation workflow, and an isolated turn advance preserving geography. Installed only the additive map-storage migration in the application database; the active game was not replaced and still returns its original map. Temporary test games/nations were confined to a separate database.

## 2026-09-19 — Approved entry experience implemented

- User read/accepted the concrete proposal and explicitly requested implementation for visual review. Marked ADR 0005 accepted and recorded its onboarding/localization timing extension to the baseline.
- Built opt-in `/client/entry`: supplied background with reduced-motion-aware fade, glass surfaces, login-only remembered audio, English/French preferences, reusable fields/image controls, step-hosted wizard and process-owned drafts. Shared map viewport primitives now serve world browsing and Homeland.
- Added session/setup contracts and JSON branches for existing login/full multipart creation, with regenerated endpoint definitions. Retained legacy HTML behavior and added a discoverable entry link. Full creation now shares a service, serializes its name/creation checks, revalidates territory selection under lock, and transactionally finalizes nation state; invocation-owned uploads are removed on failure. No schema changes.
- Translated the existing new-client shell/world/inspector UI and added a language selector without remounting current features. Server-exported content/stat titles and legacy pages remain in their existing language.
- Verification: production build, generated definition check, PHP syntax/no-DB contracts, existing Node suites and five new entry behavior checks pass. Fourteen Chromium browser journeys pass, including mobile French layout, draft/files, deep-link validation, shared map regression and same-user session recovery.
- Real HTTP verification used a unique temporary MariaDB instance and separate loopback PHP server, with isolated sessions/uploads/views. Login, French rejection, CSRF, field validation, both image uploads, connected home selection, full creation, duplicate rejection and completed-state recovery passed. Persistence/rollback tests confirmed exact territory count, image sizes, injected finalization rollback, pending-name recovery and failed-upload cleanup. Legacy HTML login/form/redirect journey passed too.
- Source/build-manifest recovery archive: `storage/app/client-backups/pre-entry-20260919.tar.gz`; previous hashed assets retained separately. Original supplied asset files retained unchanged. No active-game test nations/orders, dependency changes, default-route cutover or production service restart.
- See `entry-experience-handoff.md` for exact routes/contracts, usage, reproducible checks and browser/translation limits. Screenshots inspected; soundtrack loop quality and real devices remain review topics.
- Final checks: 45 local documentation links resolve; final built 14-journey browser regression passes. Actual-host guest entry responds HTTP 200 with loaded background and no JavaScript errors. Temporary integration PHP/MariaDB services stopped; isolated data retained under `/tmp`.

Next: user visual review and polish of the actual entry/wizard. Military workflows and other full-game views remain later scope.

## 2026-09-19 — Concrete entry-experience proposal

- User agreed to the conceptual component/instance/process structure and requested a concrete proposal, with later visual polish. Work remained documentation-only.
- Inspected the existing runtime, hosts, shell/router, saved-state, transport, theme, login routes, full nation form, narrower setup API, setup status and model finalization. Reused established architecture rather than proposing a parallel runtime or folder reorganization.
- Added `entry-experience-proposal.md` and proposed ADR 0005: persistent entry shell, four-step nation wizard, process-owned draft, audio/locale services, full-form JSON/multipart bridge, source map and E0–E4 acceptance stages. Updated the planning index and main implementation-plan pointers.
- Incorporated the user's clarification: soundtrack only during initial loading/login. Nation creation and gameplay remain silent; playback eligibility is distinct from remembered mute/volume, with transition and late-play checks in E1 acceptance.
- Recorded observed integration gaps: full-form/API parity, redirects/CSRF refresh, pending-setup recovery, missing required leader argument in the narrower territory API call, partial creation before upload failure and leader-image error field mapping. These are source findings, not live runtime tests or fixes.
- Located supplied image/audio and recorded sizes; did not copy/convert assets or audition the soundtrack. No application code, dependencies, routes/cache, build output, game data or services changed.
- Verification: reviewed proposal consistency and checked 35 local Markdown links across all 14 client-planning documents; no missing targets. No application build/tests were warranted for this documentation-only change.

Next: review the concrete proposal. Implementation, when requested, begins with E0 contract/recovery closure and E1 entry foundation; the existing read-only client remains available. Visual tuning can follow within the agreed ownership and parity boundaries.

## 2026-09-19 — Phases 1–2 implemented

- User explicitly authorized phases 1–2 after accepting the recommendations. Completed the prerequisite read-contract audit inside that scope; recorded exact contracts and limitations in `integration-contracts.md`.
- Added a separate authenticated `/client`, minimal private Blade bootstrap, generated ESM endpoint definitions, injected fetch transport, immutable WorldService snapshots, lifecycle/resource ownership, lazy loader, hosts, explicit traits and scoped notifications.
- Built the dark SCSS shell, searchable world directory, pan/zoom/high-DPI/touch map, independent layers/picking/tool responsibilities, public/owner-aware territory inspector, responsive panel/sheet and independent native dialog.
- Preserved legacy routes/generator/map/gameplay. Only small preview links were added to old screens. Named two existing read routes; no rules, permissions, schema, game data or scheduler changes.
- Added pinned Sass/browser-test/formatting dependencies and reproducible generation/test commands. Retained old hashed build assets and refreshed Laravel's route cache. Saved a private pre-change source/build archive; repository has no Git checkout.
- Verification: 27 individual Node tests, PHP no-DB contracts, ten fixture-based Chrome browser journeys, production build, and an opt-in real-data read smoke with non-read SQL blocked. Real game observed: game 2, turn 1, 600 territories, no nation for the inspected existing account. Both client and legacy guest entries redirect to login; new JS/CSS and legacy map script return HTTP 200. All 26 local documentation links checked successfully.
- Found/fixed a rejected-close-during-initialization generation race and added a regression test.
- Remaining limits: installed Chrome 127 plus emulation only; real phone/Safari/Firefox/current-browser and full authenticated HTTP user-session checks still need validation. No military actions, full nation/economy/news screens, new economy or diplomacy work.

Next: user visual review of `/client`; consult `phase-1-2-handoff.md` for usage/build/tests/backup and scope boundaries before continuing. Older entries below retain their original historical status.

## 2026-09-18 — Technical baseline and implementation plan

- Recorded accepted UI/backend separation in ADR 0004. Preserve generated endpoint knowledge, improve transport/module boundaries, and keep the legacy client compatible.
- Consolidated agreed requirements and clearly labeled new defaults in `technical-baseline.md`; generated a six-phase implementation plan and an initial current-game parity matrix.
- Confirmed a material integration gap: the current nation-creation form supports identity fields/uploads beyond the generated API's simple creation/setup flow. Preserve that form until deliberately bridged.
- Reaffirmed current-game-only scope. Historical mechanics and future diplomacy/economy/LLM features are not implementation requirements.
- Phase status: planning documents created; implementation not started. Loader/defaults remain proposed pending baseline review. No production configuration, source code, dependencies, database, or services changed.
- Validation: reviewed local routes/controllers and planning consistency; consulted official Fetch/Sass/Vite references for proposed transport/style mechanics. Checked 18 local links across 10 client-planning Markdown files with no broken links; reviewed phase/decision statuses. No build or game mutation performed.

Next: approve baseline defaults and authorize Phase 0's contract/parity audit. Older journal entries below retain their original context, not current task status.

## 2026-09-18 — Historical game feature report

- Inspected the supplied 2010-named backup without executing it. Extracted into a separate temporary directory, outside the application/public tree.
- Added `docs/game-design/historical-novus-ordo-2010-features.md`: gameplay inventory with economic fundamentals, policy catalogue, military/diplomatic features, evidence references, and qualifications for deprecated or unestablished systems.
- Kept historical findings separate from new-game decisions. No archived code was imported into the running game, and no V1 feature commitments were added.
- Key context: the old economy coupled policy and public funding to eight social/development indicators, taxable income, debt, manpower, and industrial capacity. Its conferences were public statements, not a native threaded forum.

## 2026-09-18 — Map vision and adaptive shell

- User endorsed layers / modes / interaction tools as distinct concepts.
- Recorded dark mode, SCSS with shared tokens, adaptive layout hosts, game-like panel styling, and dedicated Nation/Economy/Diplomacy/News views in decision 0003.
- Generated a desktop/mobile shell discussion image using the built-in image-generation tool. Preview only; fictional data and visual treatment are proposals, not production assets or implemented mechanics.
- Refined the earlier workspace assumption: the map is central, but dedicated full-workspace views are valid. Mobile adaptation belongs to shell/hosts rather than device checks in feature instances.
- Documentation changes only; no dependency install, application modification, build, or runtime verification.

Next: discuss the visual direction and a concrete player journey; then derive the necessary contracts. Decision 0002 (loader) remains proposed.

## 2026-09-18 — Pre-plan established

Completed:

- Recorded the user-confirmed scope: plain JavaScript, client-only Core inspiration, existing Laravel backend, and future map/diplomacy evolution.
- Inspected the existing package/build configuration, JavaScript entries, dashboard script integration, route surface, generated-client references, and map component.
- Created the backbone inventory, feature inventory, future-facing boundaries, discussion questions, and planning gates.
- Proposed native module imports with Vite and a small lazy feature registry. This is not yet an accepted implementation decision.
- Established a maintained index, decision records, and this journal.

Verification/scope:

- Changes are documentation only. No application code, configuration, dependencies, database state, or services were changed.
- Consulted official Vite, Laravel, and MDN documentation for module/build behavior. Implementation must verify exact behavior with the project's locked versions.
- No browser test, build, endpoint contract audit, or runtime proof was performed as part of pre-planning.

Open:

- Review/accept or revise decision 0002.
- Settle the high-impact questions in the pre-plan, especially workspace/device scope, rendering, trait contracts, and generated API-client integration.
- Define a minimal runtime contract and first-slice plan before authorizing implementation.

Next discussion: review the aspect inventory and loader proposal, then narrow the first slice to map selection and reusable read-only territory details.
